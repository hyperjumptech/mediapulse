{
  "name": "@workspace/eslint-plugin-strict-env",
  "version": "1.0.0",
  "type": "module",
  "description": "ESLint plugin to prevent process.env usage",
  "main": "src/index.js",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "keywords": [
    "eslint",
    "eslintplugin",
    "eslint-plugin"
  ],
  "peerDependencies": {
    "eslint": ">=9.0.0"
  },
  "devDependencies": {
    "eslint": "^9.32.0",
    "vitest": "catalog:"
  }
}
