{
  "name": "@workspace/cursor-pr-review-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runs Vitest for repo-root scripts under scripts/lib (deterministic Cursor PR rule checks).",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "catalog:",
    "vitest": "catalog:"
  }
}
