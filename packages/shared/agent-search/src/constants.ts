/** Number of results requested per query, per provider. Matches the reference. */
export const RESULTS_PER_QUERY = 10;

/** Recency window for news search (Serper `qdr:w`, Exa start date 7 days back). */
export const RECENCY_DAYS = 7;
