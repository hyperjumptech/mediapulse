# Prisma uses this connection string to connect to the PostgreSQL database.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
# Example: postgresql://postgres:mypassword@localhost:5432/mydb?schema=public
DATABASE_URL=postgresql://mediapulse:mediapulse@localhost:5432/mediapulse?schema=public # required
DATABASE_CERT_BASE64=

# DataQueue (Hermes scheduler). Same DB OK; run migrations with --schema dataqueue. Optional outside Hermes. Read more here https://docs.dataqueue.dev/usage/database-migration
PG_DATAQUEUE_DATABASE=postgresql://mediapulse:mediapulse@localhost:5432/mediapulse?schema=dataqueue&search_path=dataqueue # required
