# Port this agent listens on (query-analysis: 4011)
PORT=4011 #number #default
# URL endpoint for communicating with agent-data-api
AGENT_DATA_API_URL="http://localhost:8081" # required
# URL of the agent-auth-api service used to verify API keys
AGENT_AUTH_API_URL="http://localhost:8080" # required

# Optional: when all three are set, agent registers itself with the registry on startup
AGENT_REGISTRY_URL="http://localhost:8082"
AGENT_REGISTRY_API_KEY=
AGENT_PUBLIC_URL="http://localhost:4011"