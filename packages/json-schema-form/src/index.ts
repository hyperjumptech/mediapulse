/**
 * @workspace/json-schema-form
 *
 * Renders a form from a JSON Schema. Supports nested objects and arrays.
 * Used by Hermes for agent config forms and reusable elsewhere.
 */

export { SchemaForm } from "./schema-form";
export type { JsonSchema, JsonSchemaTypeName, SchemaFormProps } from "./types";
