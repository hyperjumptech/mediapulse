---
title: Communication Between Parts
description: How Hermes, hermes-worker, agents, and the auth/data/registry APIs communicate — JWT verification end to end
icon: MessageSquare
---

This page explains **how the different parts of the system talk to each other**: who calls whom, with which credentials, and how verification works. For a high-level picture of components, see [Architecture](/architecture). For environment variables and production setup, see [Production](/production).

## Auth mechanisms (overview)

| Flow                                            | Credential on the wire                                                                                                              | Verified by                                                         |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| **Minting** (Hermes / worker → agent-auth-api)  | Long-lived **scheduler API key** in `Authorization: Bearer …` on `POST /api/token`                                                  | Key lookup in DB (purpose `scheduler`)                              |
| **Invocation** (Hermes / worker → agent)        | Short-lived **JWT**                                                                                                                 | Agent calls `POST /api/verify`                                      |
| **Agent → agent-data-api / agent-registry-api** | **Same JWT** the agent received for the run (forwarded), or a **new JWT** minted with the scheduler key (e.g. startup registration) | Each API uses `POST /api/verify` via `@workspace/agent-auth-client` |
| **Dashboard / admin** (when applicable)         | Raw API key on some routes                                                                                                          | `POST /api/verify-api-key`                                          |

Invocation proves the scheduler may run the step. **agent-data-api** and **agent-registry-api** do not accept raw API keys from agents for normal requests—they expect a **JWT** and verify it the same way agents do.

---

## Invocation flow: Hermes or hermes-worker → Agent

When a pipeline runs (from the dashboard or on a schedule), the caller **never** sends the raw scheduler API key to agents. It first gets a short-lived JWT from agent-auth-api, then sends that JWT to the agent.

**Where the scheduler key comes from:** An **admin** must create a long-lived API key in the **Hermes dashboard** (e.g. **API keys** in the sidebar). When creating the key, set **purpose** to `scheduler`. The raw key is shown once at creation; that value is what you set as `AGENT_API_KEY` in the environment for Hermes and hermes-worker. Hermes and hermes-worker use this key only to call `POST /api/token`; they never send it to agents.

<Mermaid
  chart={`sequenceDiagram
  participant HermesOrWorker as "Hermes / hermes-worker"
  participant AuthAPI as "agent-auth-api"
  participant Agent as Agent

HermesOrWorker->>AuthAPI: POST /api/token with Bearer scheduler_api_key
AuthAPI->>AuthAPI: Validate key, purpose scheduler
AuthAPI->>HermesOrWorker: 200 + JWT, expiresIn
HermesOrWorker->>Agent: POST / with Bearer JWT + body
Agent->>AuthAPI: POST /api/verify with Bearer JWT
AuthAPI->>AuthAPI: Verify signature and expiry
AuthAPI->>Agent: 200 valid true
Agent->>Agent: Run logic
Agent->>HermesOrWorker: 200 response
`}
/>

**Steps:**

1. **Hermes** (manual run) or **hermes-worker** (scheduled run) is configured with a long-lived **scheduler API key**. This key must be **created by an admin in the Hermes dashboard** (API keys → Create, purpose `scheduler`). The admin copies the key once at creation and sets it as `AGENT_API_KEY` in the env for Hermes and hermes-worker. The caller then uses it to call **agent-auth-api** `POST /api/token` with `Authorization: Bearer <scheduler_key>`.
2. **agent-auth-api** checks the key in the DB (active, purpose `scheduler`), signs a JWT with `AGENT_AUTH_JWT_SECRET`, and returns `{ token, expiresIn }` (e.g. 15 minutes).
3. Hermes/hermes-worker **caches** the JWT and uses it for all agent requests until close to expiry, then fetches a new one.
4. For each pipeline step, the caller sends `POST` to the agent URL with `Authorization: Bearer <jwt>` and the step input/config.
5. The **agent** (via `@workspace/agent-runtime` bearer middleware) receives the request, extracts the Bearer token, and calls **agent-auth-api** `POST /api/verify` with that token.
6. **agent-auth-api** `POST /api/verify` is **JWT-only**: it verifies signature and expiry. If the token is not a JWT or is invalid, it returns 401. The agent rejects the request unless it gets 200.
7. The agent runs its logic and returns the result.

**Important:** The scheduler API key is only ever sent to agent-auth-api. Agents only ever see the JWT, so a leaked JWT expires in minutes and the key can be revoked in the dashboard without sending it to every agent.

---

## Agent → agent-data-api / agent-registry-api (JWT)

Agents call **agent-data-api** (read/write pipeline data) and optionally **agent-registry-api** (register). Both services use **bearer JWT** auth: the same **`Authorization: Bearer <jwt>`** the agent received for the pipeline step is forwarded to agent-data-api via `@workspace/agent-data-api-client`. For **registry** at startup (outside a pipeline), the agent typically mints a JWT using the **scheduler** `AGENT_API_KEY` and `POST /api/token`, then calls register with that JWT.

<Mermaid
  chart={`sequenceDiagram
  participant Agent as Agent
  participant DataAPI as "agent-data-api"
  participant RegistryAPI as "agent-registry-api"
  participant AuthAPI as "agent-auth-api"

Agent->>DataAPI: GET/POST /api/... with Bearer JWT
DataAPI->>AuthAPI: POST /api/verify with Bearer JWT
AuthAPI->>DataAPI: 200 valid
DataAPI->>Agent: 200 + data

Agent->>RegistryAPI: POST /api/agents/register with Bearer JWT
RegistryAPI->>AuthAPI: POST /api/verify with Bearer JWT
AuthAPI->>RegistryAPI: 200 valid
RegistryAPI->>Agent: 200 registered
`}
/>

**Steps:**

1. During a pipeline run, the agent forwards the **invocation JWT** to **agent-data-api** (SDK passes the token from the request context).
2. **agent-data-api** and **agent-registry-api** call **agent-auth-api** `POST /api/verify` with the Bearer token (via `verifyTokenViaAuthApi`).
3. **agent-auth-api** verifies the JWT signature and expiry. On success, the data or registry handler runs.

JWT verification is centralized in **agent-auth-api**; data and registry APIs do not store keys for agent traffic.

---

## Summary diagram

<Mermaid
  chart={`flowchart TB
  subgraph inv ["Invocation auth"]
    H["Hermes / hermes-worker"]
    H -->|"Bearer scheduler_key"| T["POST /api/token"]
    T -->|"JWT"| H
    H -->|"Bearer JWT"| A["Agent"]
    A -->|"Bearer JWT"| V["POST /api/verify"]
    V -->|"valid?"| A
  end

subgraph svc ["Agent to platform APIs (JWT)"]
A2["Agent"]
A2 -->|"Bearer JWT"| D["agent-data-api"]
A2 -->|"Bearer JWT"| R["agent-registry-api"]
D -->|"Bearer JWT"| V2["POST /api/verify"]
R -->|"Bearer JWT"| V2
V2 -->|"valid?"| D
V2 -->|"valid?"| R
end

subgraph auth_api ["agent-auth-api"]
T
V
VK
V2
end
`}
/>

- **Invocation:** Scheduler (Hermes/hermes-worker) → `POST /api/token` → JWT → agent → `POST /api/verify`. Raw scheduler key never goes to the agent.
- **Data / registry:** Agent → data or registry API with **JWT** → `POST /api/verify`. Same verification path as invocation.
- **verify-api-key:** Used for flows that present a **raw API key** (e.g. some dashboard operations), not for normal agent→data-api traffic.

---

## Where to configure what

| Component          | Needs                                                                          | Used for                                                                                                                                                                                             |
| ------------------ | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **agent-auth-api** | `AGENT_AUTH_JWT_SECRET`                                                        | Signing/verifying JWTs (`/api/token`, `/api/verify`). Required for JWT flow.                                                                                                                         |
| **Hermes**         | `AGENT_AUTH_API_URL`, `AGENT_API_KEY`                                          | Minting JWT when user runs a pipeline from the dashboard. `AGENT_API_KEY` = scheduler key **created by admin in Hermes dashboard** (API keys, purpose `scheduler`).                                  |
| **hermes-worker**  | `AGENT_AUTH_API_URL`, `AGENT_API_KEY`                                          | Minting JWT for scheduled pipeline runs. Same scheduler key as Hermes; **create it in the Hermes dashboard** (API keys, purpose `scheduler`).                                                        |
| **Agent**          | `AGENT_AUTH_API_URL`                                                           | Verifying invocation JWT via `POST /api/verify`.                                                                                                                                                     |
| **Agent**          | Forwarded **JWT** (and optional scheduler key to mint a JWT for registry only) | Calling agent-data-api / agent-registry-api: same JWT as invocation, verified with `POST /api/verify`. Optional `AGENT_API_KEY` (scheduler) only to call `POST /api/token` for startup registration. |

For a full list of env vars per app, see [Production](/production).
