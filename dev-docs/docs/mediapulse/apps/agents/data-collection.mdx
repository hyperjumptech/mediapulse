---
title: Data Collection Agent
description: Agent that collects web data and search results for a ticker and stores them via agent-data-api
icon: Search
---

## Purpose

The data-collection agent fetches **search queries** for a ticker from agent-data-api, runs **web search** (e.g. Serper) and **page content extraction** (e.g. Jina), then stores the collected **data sources** back via agent-data-api for the content-generation agent.

## Features

- **Input:** `tickerId` and optional `timeWindow` (start/end datetimes). Receives bearer token from Hermes.
- **Fetches** search queries for the ticker from agent-data-api (`/api/v1/data-collection`).
- **Web search** — Uses Serper (or similar) for search results; requires `SERPER_API_KEY`.
- **Web fetch** — Uses Jina (or similar) to get page content; requires `JINA_API_KEY`.
- **Stores** collected pages/sources via agent-data-api for content-generation.
- **Config schema** — Exposes a config schema for pipeline/UI (e.g. search engine settings).

## Entrypoint

- **Main:** `src/index.ts` — Exports a Bun/Hono-compatible `fetch` and `port` (default **4001**).

## Port

- **4001** (default from `env.PORT`).

## Workspace dependencies

- `@workspace/agent-auth-client` — Token verification.
- `@workspace/agent-runtime` — `createAgentApp`.
- `@workspace/agent-types` — `DataCollectionInput` and related types.
- `@workspace/agent-data-api-client` — Optional typed client for agent-data-api.
- `@workspace/env` — Via `@workspace/env/agents-data-collection`.
- `@workspace/logger` — Logging.
- `@workspace/utils` — Helpers (e.g. `sleep`).

## Related docs

- [agent-data-api](/mediapulse/apps/agent-data-api) — Read/write data-collection data.
- [Packages: agent-runtime](/hermes/packages/agent-runtime), [agent-types](/hermes/packages/agent-types), [utils](/hermes/packages/utils).
