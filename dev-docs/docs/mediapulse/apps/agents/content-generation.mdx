---
title: Content Generation Agent
description: Agent that generates newsletter content from collected data sources using OpenAI
icon: FileText
---

## Purpose

The content-generation agent takes **selected data sources** (scored by the analysis agent and served by agent-data-api) and produces a **newsletter** (subject, content, optional description) using OpenAI. It then stores the result via agent-data-api for the delivery agent to consume.

## Features

- **Input:** `tickerId` (and optional fields). Receives bearer token from Hermes.
- **Fetches** selected high-relevance sources for the ticker from `GET /api/v1/content-generation` (rows where `article_relevance.selected = true` and scored today).
- **Calls OpenAI** to generate structured newsletter content (subject, executive summary, top news items). The API key, optional API base URL, and model id are **not** read from environment variables; they come from the **agent config** selected for the pipeline step in Hermes (Dashboard → Agent configs), so admins can manage credentials centrally (optionally using [variable expansion](/hermes/agent-hermes-contract#variables-in-config-and-params) for secrets).
- **Stores** generated content via agent-data-api for the delivery agent.
- **Skips** with a clear message if no data sources are found.

## Entrypoint

- **Main:** `src/index.ts` — Exports a Bun/Hono-compatible `fetch` and `port` (default **4002**).

## Port

- **4002** (default from `env.PORT`).

## Workspace dependencies

- `@workspace/agent-auth-client` — Token verification.
- `@workspace/agent-runtime` — `createAgentApp`.
- `@workspace/agent-data-api-client` — Typed API client for content-generation endpoints.
- `@workspace/agent-types` — `DataSourceRecord` and related types.
- `@workspace/env` — Via `@mediapulse/env/agents-content-generation` (agent-data-api URL, auth URL, port, optional registry). OpenAI settings use **`config.openai`** from Hermes: required **`apiKey`**, optional **`baseUrl`**, optional **`model`** (defaults to `gpt-4o-mini`), plus **`temperature`**, **`maxTokens`**, and **`timeoutMs`** as defined in the agent config schema.
- `@workspace/logger` — Logging.

## Related docs

- [agent-data-api](/mediapulse/apps/agent-data-api) — Read/write content-generation data.
- [Packages: agent-runtime](/hermes/packages/agent-runtime), [agent-types](/hermes/packages/agent-types).
