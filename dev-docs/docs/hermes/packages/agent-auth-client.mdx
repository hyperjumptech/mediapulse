---
title: agent-auth-client
description: Client for verifying bearer tokens via agent-auth-api
icon: Key
---

## Purpose

`@workspace/agent-auth-client` exposes helpers to verify credentials via agent-auth-api. **`verifyTokenViaAuthApi`** is used by agents, **agent-data-api**, and **agent-registry-api** to validate the same short-lived **invocation JWT** (`POST /api/verify`).

## Exports

- **`verifyTokenViaAuthApi(token, authApiUrl)`** — `Promise<boolean>`. Calls `POST ${authApiUrl}/api/verify` with `Authorization: Bearer ${token}`; returns `true` if status is 200, `false` otherwise (401, network error, etc.).
- **`createAgentTokenClient({ authApiUrl, credential, fetchFn? })`** — Returns `{ getToken() }` to mint JWTs via `POST /api/token`. Pass **`HERMES_INTERNAL_API_KEY`** from Hermes worker/dashboard, or a **domain integration** API key (hash stored on `encrypted_payload`), as `credential` (used by product agents and registration flows).

## Consumers

agent-data-api and agent-registry-api (bearer middleware), agent-runtime (default `verifyToken` when `authApiUrl` is set), and pipeline agents that forward the invocation JWT to downstream APIs.
