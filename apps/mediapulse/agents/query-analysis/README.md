# query-analysis agent

Generates versioned search-query sets per ticker by combining deterministic baseline templates with LLM-generated suggestions, then persists the active set through `agent-data-api`.

## Development

```bash
pnpm --filter @mediapulse/query-analysis dev
```
