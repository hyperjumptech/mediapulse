/** Stable agent identity recorded on collection runs and stamped into run snapshots. */
export const PAGE_COLLECTION_AGENT_ID = "page-collection" as const;
export const PAGE_COLLECTION_AGENT_VERSION = "2.0.0" as const;
