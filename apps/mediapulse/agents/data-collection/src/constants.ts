/** Stable agent identity recorded on collection runs and stamped into run snapshots. */
export const DATA_COLLECTION_AGENT_ID = "data-collection" as const;
export const DATA_COLLECTION_AGENT_VERSION = "1.0.0" as const;
