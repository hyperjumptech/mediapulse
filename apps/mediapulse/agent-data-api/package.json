{
  "name": "agent-data-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ./.env.local -- env PORT=8081 bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outfile=./dist/server.js --target=bun",
    "type:check": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@workspace/agent-auth-client": "workspace:*",
    "@workspace/agent-data-api-contract": "workspace:*",
    "@workspace/agent-types": "workspace:*",
    "@workspace/api-utils": "workspace:*",
    "@workspace/env": "workspace:*",
    "@workspace/database": "workspace:*",
    "@workspace/logger": "workspace:*",
    "hono": "catalog:",
    "hono-pino": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@workspace/typescript-config": "workspace:*",
    "dotenv-cli": "catalog:",
    "typescript": "catalog:",
    "vitest": "^4.0.18"
  }
}
