{
  "name": "@hermes/agent-registry-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ./.env.local -- env PORT=8082 bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outfile=./dist/server.js --target=bun",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@workspace/agent-auth-client": "workspace:*",
    "@workspace/api-utils": "workspace:*",
    "@hermes/orchestration-database": "workspace:*",
    "@hermes/env": "workspace:*",
    "@workspace/logger": "workspace:*",
    "hono": "catalog:",
    "hono-pino": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv-cli": "catalog:",
    "vitest": "catalog:"
  }
}
