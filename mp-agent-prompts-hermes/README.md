# Hermes SchemaForm visual proof (#478–482)

These placeholder images document the intended capture surface (agent config JSON Schema / SchemaForm) for optional LLM prompt fields.

Local repro: run `pnpm dev:hermes` from the repo root, open an agent that exposes `configSchema`, and confirm multiline `prompts` fields render with titles and descriptions from Zod `.describe()`.
